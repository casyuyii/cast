name: cast

services:
  nginx:
    build:
      context: .
      dockerfile: ./nginx.prod.Dockerfile
    ports:
      - "80:80"
    depends_on:
      - client
      - server
    networks:
      - public

  client:
    build:
      context: ..
      dockerfile: ./docker/client.Dockerfile
      args:
        NEXT_PUBLIC_BACK_END_API_URL: http://casyu.me/api
        NEXT_PUBLIC_FRONT_END_URL: http://casyu.me
    environment:
      PORT: 80
    depends_on:
      - server
    networks:
      - public

  server:
    build:
      context: ..
      dockerfile: ./docker/server.Dockerfile
    environment:
      MONGO_URI: mongodb://mongodb:27017
      PORT: 8080
    depends_on:
      - mongodb
    networks:
      - public
      - internal

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - internal

networks:
  public:
  internal:
    internal: true

volumes:
  mongodb_data:
  mongodb_config: